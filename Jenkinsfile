pipeline{
    agent any

    stages{
        stage ('Compilar') {
            steps {
                    echo "Iniciando processo de build..."
                    bat 'cd Teste_Jenkins'
            }
        }
    }
}